- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bashrc: bashrc
    ~/.vimrc: vimrc
    ~/.mailcap: mailcap
    ~/.muttrc: muttrc
    ~/.mutt/aliases: mutt/aliases
    ~/.latexmkrc: latexmkrc
    ~/.gitconfig: .gitconfig
    ~/.gitignore: .gitignore
    ~/.config/espanso: espanso
