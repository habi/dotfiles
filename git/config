[user]
    name = David Haberthür
    email = email@davidhaberthuer.ch
[alias]
    dt = difftool
    # pretty and short output
    l = log --graph --pretty=format:'%C(red)%h%Creset%C(green)%d%Creset %C(white dim)%s%Creset %C(white)%an, %ar%Creset'
    # view all branches in short form
    ll = !git l --all
    # short status
    s = status -s
[core]
    editor = vim
    excludesfile = ~/.gitignore
    whitespace = blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
    autocrlf = input
    attributesfile = ~/.gitattributes
	filemode = false
	filemode = false
	hooksPath = .git/hooks
[apply]
    whitespace = fix
[color]
    ui = true
[help]
    autocorrect = 1
[init]
    templatedir = ~/.git_template
	defaultBranch = main
[filter "lfs"]
    process = git-lfs filter-process
    required = true
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
[safe]
	directory = /home/habi/P/Documents/EAWAG
	directory = /home/habi/P/Documents/Hearts-Melly
	directory = /home/habi/P/Documents/Publications/Ruslan Bone/manubot
