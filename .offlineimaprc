[general]
# This specifies where offlineimap is to store its metadata.
metadata = ~/.offlineimap
# This variable specifies which accounts are defined.
accounts = PSI
maxsyncaccounts = 1
[Account PSI]
# These settings specify the two folders that you will be syncing.
localrepository = Local
remoterepository = Remote
autorefresh = 5
quick = 10
[Repository Local]
type = Maildir
localfolders = /scratch/PSI-mail/
sep = /
restoreatime = no
[Repository Remote]
type = IMAP
remotehost = mail.ethz.ch
ssl = yes
remoteuser = psich/haberthuer
maxconnections = 2
holdconnectionopen = no
subscribedonly = no
folderfilter = lambda foldername: foldername not in ['Kalender']
